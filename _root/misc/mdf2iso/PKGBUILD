name=mdf2iso
version=0.2.2
release=1
license=gpl
group=group/name
depends=()
optdeps=()
sources=(http://freshmeat.net/redir/mdf2iso/54376/url_bz2/$name-$version-src.tar.bz2)

build() {
    mkdir -p $PKG/usr/bin
    cd $name-$version-src
    make
    cp mdf2iso $PKG/usr/bin
}

